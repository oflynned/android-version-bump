name: 'CI/CD'
on:
  push:
    branches:
      - master

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          npm ci
#          npm run lint
          npm test
          npm run build
          npm run package
